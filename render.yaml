services:
  - type: web
    name: fileforge
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "3000"
      - key: HOSTNAME
        value: "0.0.0.0"
      - key: NODE_ENV
        value: "production"
    healthCheckPath: /
